import { TypingText } from "../components/typingText"

export const Footer = () => {
	return (
		<p className="body">
			<TypingText text="Change is the only constant" typingQueuePosition={1} />
		</p>
	)
}
